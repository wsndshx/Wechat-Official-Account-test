version: 1.0.{build}
platform:
- x86
- x64
- ARM
build: off

clone_folder: /usr/go/src/github.com/$username/$project

environment:
  GOPATH: /usr/go/

stack: go 1.14

before_test:
  - go vet ./...

test_script:
  - go test ./...